# Streamlit Community Cloud reads requirements.txt for dependency installation.
# Keep this in sync with pyproject.toml [project].dependencies.

duckdb>=1.4.0
httpx>=0.28.1
polars>=1.34.0
requests>=2.32.0
streamlit>=1.50.0
streamlit-authenticator>=0.4.0
